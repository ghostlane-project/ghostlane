### Memory

On iOS, going to the background with a sheet or dialog open used to leave the screen's overlay behind, so the app kept about twice the memory it should have and the phone was shorter for it. The overlay is now removed together with the rest of the screen.
